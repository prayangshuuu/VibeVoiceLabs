# Python 3.10+ — keep in sync with repo root requirements.txt for Docker
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0
python-multipart>=0.0.9

vibevoice[streamingtts] @ git+https://github.com/microsoft/VibeVoice.git
